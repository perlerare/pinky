extends layout

block content
	if user
		if errors
			each error in errors
				.alert.alert-danger.alert-dismissible= error.msg
		if friends
				a=friends.name
	else
		include login
	//- script.
	//- 	var socket = io('//localhost:3000');
	//- 	socket.on('socketToMe', function (data) {
	//- 		console.log(data);
	//- 	});
